$if(title)$
\title{$title$}
$endif$
\authorsnames{$for(authors)$$it.name.literal$$sep$, $endfor$}
\makeatletter
\gdef\ccr@authorblock{%
  \noindent
  $for(by-author)$$it.name.literal$$if(it.affiliations)$\textsuperscript{$for(it.affiliations)$\@alph{$it.number$}$sep$,$endfor$}$endif$$sep$, $endfor$%
  \par\vspace{0.5em}%
  {\small\itshape\parskip=0pt\parindent=0pt%
  $for(affiliations)$\textsuperscript{\@alph{$it.number$}}\,$it.name$\par
  $endfor$}%
}
\makeatother
